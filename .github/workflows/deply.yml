name: Deploy to OpenShift

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install oc CLI
        run: |
          if ! command -v oc &> /dev/null; then
            echo "oc not found, downloading..."
            curl -LO https://mirror.openshift.com/pub/openshift-v4/clients/ocp/stable/openshift-client-linux.tar.gz
            tar xzf openshift-client-linux.tar.gz
            sudo mv oc /usr/local/bin/
            sudo mv kubectl /usr/local/bin/
          else
            echo "oc is already installed, skipping."
          fi

      - name: Login to OpenShift
        run: |
          oc login ${{ secrets.OPENSHIFT_SERVER }} \
            --token=${{ secrets.OPENSHIFT_TOKEN }} \
            --insecure-skip-tls-verify=true

      - name: Trigger new build
        run: |
          oc project cicd-demo
          # 빌드 시작 및 완료 대기
          oc start-build cicd-demo-app-git --follow

      - name: Trigger Deployment update
        run: |
          # 1. annotation에 현재 시간을 주입하여 설정을 변경된 것으로 간주하게 만듭니다.
          # 2. 이 과정을 통해 OpenShift는 무조건 파드를 재배포(Rolling Update)합니다.
          oc patch deployment/cicd-demo-app-git -p "{\"spec\":{\"template\":{\"metadata\":{\"annotations\":{\"date\":\"$(date +%s)\"}}}}}"
         
          # 3. 배포 시작
          oc rollout latest deployment/cicd-demo-app-git

      - name: Verify rollout
        run: |
          # 배포 상태를 최종 확인합니다.
          oc rollout status deployment/cicd-demo-app-git
